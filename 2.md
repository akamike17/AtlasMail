CORRECCIÓN FINAL ATLASMAIL — defectos ya auditados. NO AUDITES y NO amplíes alcance.

Corrige exactamente lo siguiente:

1. BackupService.RestoreAsync:
   actualmente restaura DB antes del message-store y puede dejar un restore parcial.
   Implementa restore seguro mediante validación/staging previo y publicación consistente.
   Si cualquier MIME/hash/storeKey falla, NO debe quedar la DB publicada apuntando a un store parcial.
   Añade rollback/estrategia equivalente verificable.

2. FileSystemMessageStore.SaveWithKeyAsync:
   elimina File.WriteAllBytes directo bajo lock.
   Valida siempre storeKey mediante Resolve.
   Escribe a archivo temporal en el mismo filesystem, finaliza/flush, y publica mediante rename/replace atómico.
   Ante interrupción no debe quedar MIME truncado como archivo válido.

3. SMTP AUTH rate-limit:
   elimina el lock global sobre AuthFailures.
   Haz estado thread-safe por IP.
   Expira y ELIMINA entradas antiguas para impedir crecimiento ilimitado.
   Mantén ventana/límite configurables.
   Añade tests concurrentes y prueba explícita:
   N fallos permitidos → siguiente bloqueado → expiración → vuelve a permitir.
   IP A no debe bloquear IP B.

4. SMTP DATA:
   después de MaxMessageBytes no almacenes más datos (comportamiento actual correcto),
   pero tampoco permitas un stream DATA infinito.
   Añade límite absoluto/timeout de DATA y cierre SMTP seguro al excederlo.
   Prueba cliente que continúa enviando después del límite y nunca manda ".":
   el servidor debe terminar la sesión dentro del límite configurado sin crecimiento de memoria.

5. Backup/restore tests:
   añade pruebas AUTOMÁTICAS que demuestren:
   - clave original preservada;
   - MIME idéntico byte a byte;
   - DB + store restaurados;
   - hash corrupto no modifica destino;
   - storeKey inválido/path traversal no modifica destino;
   - fallo simulado durante write-back no deja restore parcialmente publicado;
   - restore exitoso permite volver a leer el mensaje.

6. Corrige TEST_EVIDENCE:
   NO marques brute-force AUTH, restore atómico ni ningún comportamiento como PASS hasta que exista prueba ejecutada.
   Distingue prueba automatizada de smoke manual.
   No cambies EXTERNAL/NOT PROVEN a PROVEN sin evidencia.

7. Cierra las deudas de seguridad locales que el propio repo todavía reconoce:
   - STARTTLS/TLS del servidor SMTP e IMAP;
   - impedir AUTH/LOGIN en claro cuando TLS sea obligatorio;
   - sanitización segura del HTML de correos y prueba XSS hostil.
   MFA/TOTP sólo impleméntalo si forma parte del requisito local ya especificado y puede probarse completamente; de lo contrario mantenlo explícitamente pendiente, sin afirmar completitud.

Después:
dotnet restore
dotnet build -c Release
dotnet test -c Release

Ejecuta además los E2E SMTP/IMAP/backup-restore existentes.

NO agregues nuevas fases.
NO hagas refactors cosméticos.
NO declares nada probado sin ejecución.
NO te detengas después del primer fix.

Cuando TODO lo anterior pase:
actualiza únicamente la documentación afectada,
haz commit + push,
muestra SHA final, git status, build y resultados exactos de tests.